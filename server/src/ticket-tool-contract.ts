export const REMY_TOOL_INSTRUCTIONS = `Use Remy as the source of truth for the user's workspaces, threads, agents, and tickets. A project or repository the user wants registered is a Remy workspace. When the user names a ticket key, read it before planning or changing code; a linked thread may omit the key. Use the thread tools when the user asks you to delegate, start another thread, continue one, or inspect its result. Use the shared browser for click and visual QA so the person can watch and take control, and check both desktop and mobile layouts when the interface is responsive. Use run_with_environment when a command needs the workspace's configured environment; never try to read or print those values. Keep product scope, findings, and status on the ticket. Do not claim Remy changed unless a tool confirms it.`;
